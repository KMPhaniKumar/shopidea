# Module: ecs-cluster

ECS cluster with Container Insights enabled. Capacity provider linkage happens in `ec2-asg`.

Filled in during **Phase 1**.

## Inputs (planned)
- `environment` (string)
- `cluster_name` (string)

## Outputs (planned)
- `cluster_name`
- `cluster_arn`
